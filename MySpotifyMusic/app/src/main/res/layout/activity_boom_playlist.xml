<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/playlistContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/appBackground" />

    <LinearLayout
        android:id="@+id/playlist_empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/color400"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/add_boom_playlist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_alarm_add_white_24dp"
        android:layout_margin="16dp"
        app:layout_anchor="@+id/playlistContainer"
        app:layout_anchorGravity="bottom|end"
        app:layout_behavior="com.player.boom.ui.musiclist.FloatingScrollBehavior"/>

</android.support.design.widget.CoordinatorLayout>